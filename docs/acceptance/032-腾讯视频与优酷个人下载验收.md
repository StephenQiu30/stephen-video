# 032 腾讯视频与优酷个人下载验收

状态：部分完成。优酷普通样本完成下载，腾讯普通样本完成解析但下载限速未解决；两平台 VIP 未完成真实会话验收。日期：2026-09-07。

## 真实样本结果

使用临时 Linux Runner 容器、现有 `video-server:local` 镜像绑定当前源码，复用既有隔离出口代理。测试调用真实 MediaRunnerService；没有重建生产镜像，没有经过业务 API、Worker、MinIO 链路或重启现有业务容器。

| 平台 | 样本与解析结果 | 文件结果 |
| --- | --- | --- |
| 腾讯 | 用户提供 `https://v.qq.com/x/cover/mzc00200fr1ry1o/m00441h6knj.html`；标题龙族_0；原始时长 2955 秒；720p / 486p，HEVC/AAC | 匿名元数据及下载前复核通过；媒体分段约 1 KB/s，完整下载未通过，已停止测试并清理部分文件 |
| 优酷 | 固定公开样本 `https://v.youku.com/v_show/id_XOTUxMzg4NDMy.html`；原始时长 702.08 秒；1080p、720p、540p、360p | 1080p H264/AAC MP4 完整下载；1920×1080，702.126 秒，384475576 字节；通过 Runner 最终制品校验 |

优酷文件位于测试机器 `~/Downloads/FrameFetch/XOTUxMzg4NDMy.mp4`，SHA-256 为 `1df99a497980430a0353ea67af901b62979c11b04a2073f92083c448b3cd2c75`。FFmpeg 对 0、350、697 秒位置各解码 3 秒，退出码均为 0；这属于自动解码检查，不替代人工观看。

腾讯当前样本匿名可取得完整时长与格式，因此不能作为 VIP 成功证据。腾讯页面的旧 pinia 依赖已移除，当前 startup/vinfo_proxy 元数据、播放接口内联 HLS 和有限 Range 编码探测均已实现。读取完整清单曾导致 inspection_timeout；内联清单加前缀探测后可以完成真实解析。下载限速在多种 HTTP 实现中均复现，[yt-dlp issue 8171](https://github.com/yt-dlp/yt-dlp/issues/8171) 有相同约 1 KB/s 报告；没有证明提供登录后一定能解除限速。

## 自动化与运行边界

覆盖单平台文件会话在新 Store 中恢复且源文件不变、无需桌面代理、UPS cna 复用、腾讯元数据 vid/cid/目标主机校验、试看与不完整分段拒绝、内联清单总时长与 DRM 检查、账号 token 仅进入指定 API、CDN 回退、8 KiB 前缀上限与清理，以及最终文件完整性验证。原始完整时长不会被短分段探测覆盖。

全量后端 pytest：1611 passed、2 skipped。跳过项为未提供隔离 MinIO 的上传上限集成测试及 macOS 不具备的 Linux O_PATH 行为。Ruff 通过，mypy 522 个源文件通过；本机与生产 Compose（含两个个人 profile）静态解析通过，修改文档链接和 git diff --check 通过。新回归测试自动进入现有 GitHub Actions；固定公开探针集增加腾讯样本，探针登记不代表平台已验证可用。

## 剩余验收

- 提供两平台有效会话文件的本机路径，以及实际会员样本（优酷链接尚未提供）；无需在聊天中粘贴 Cookie 内容或密码。
- 在持有会话的部署中完成两平台 VIP Inspect→Download→Artifact→本地播放，记录完整时长、大小与人工首中尾播放。
- 完成目标 Linux 服务器容器重建、新机器私有迁移后的真实下载验证，并验证业务 API、Worker、MinIO 链路。

当前没有确认所有普通/VIP/SVIP 内容、画质或地区都可用；没有把试看或未完成文件生成成功制品。见[设计](../design/032-腾讯视频与优酷个人下载设计.md)、[需求](../prd/032-腾讯视频与优酷个人下载需求.md)、[计划](../plans/032-腾讯视频与优酷个人下载计划.md)。
