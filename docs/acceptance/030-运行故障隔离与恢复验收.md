# 030 运行故障隔离与恢复验收

- 状态：In Progress；本地实现验证通过，目标环境验收待完成
- Design：[设计](../design/030-运行故障隔离与恢复设计.md)
- PRD：[需求](../prd/030-运行故障隔离与恢复需求.md)
- Plan：[计划](../plans/030-运行故障隔离与恢复计划.md)

- [x] Runner 不可用不影响 API readiness，DB/MinIO/MQ/Valkey 故障仍失败。
- [x] 两套业务 Compose 不把 Provider 健康作为核心 API/下载 Worker/Canary 启动门槛。
- [x] 安装标记存在但无代理响应时 probe 失败；恢复响应后成功。
- [x] probe 不导出 Cookie，拒绝畸形、跨 Provider、篡改响应；超时/取消清理队列。
- [x] 下载 Worker 与 Runner 停止窗口覆盖 Worker 排空预算。
- [x] 现有 lease/Outbox/幂等与针对性回归通过。
- [x] ruff、mypy、必要后端测试和业务/CI Compose 静态解析通过。
- [ ] 目标环境宿主代理/Runner 同步升级后的真实重启恢复验证。

最后一项必须有目标环境证据，不能由单元测试或静态 Compose 检查替代。

## 2026-09-07 本地证据

- 原始 Compose 回归测试在修改前因媒体依赖阻塞而失败；修改后通过。
- 相关回归：149 passed、1 skipped；覆盖代理队列实际加密往返、无响应后恢复、SQL 租约回收及旧执行者隔离。
- 后端全量：1539 passed、2 skipped（41.72 秒）。其后新增的核心依赖失败与跨请求 probe 响应校验：3 passed。
- 跳过原因：未提供隔离 MinIO 上传边界端点；macOS 不提供 Linux O_PATH。未将跳过项计为通过。
- `ruff check app tests` 通过；`mypy app` 通过（520 个源码文件）。
- 本机业务、生产业务和 CI 夹具 Compose `config --quiet` 通过。未在本机启动 CI 基础服务。
- 工作流 YAML、Bash 语法及三个内嵌 Python 片段静态解析通过。新增 hosted CI 停 Runner/API 冷启动步骤尚未远端执行。
- 未重建或重启现有业务容器、未调用真实平台下载；现有代理刷新请求仍采用原格式。新 Runner probe 功能需要目标镜像部署后验收。

## 尚待目标环境验证

先更新宿主代理代码/依赖，再部署新 Runner 镜像；若安装路径改变，重新登记 LaunchAgent。
验证宿主机重启、GUI 未登录、代理繁忙与恢复、下载进行中服务停止、真实对象上传和完整媒体交付。
这些运行证据以及远端 CI 成功记录补齐前，整体结论不能标为 production passed。
