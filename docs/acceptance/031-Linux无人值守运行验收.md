# 031 个人部署重启与换机验收

- 状态：In Progress；本地与隔离运行验证见下方，真实平台待完成。
- Design：[设计](../design/031-Linux无人值守运行设计.md)
- PRD：[需求](../prd/031-Linux无人值守运行需求.md)
- Plan：[计划](../plans/031-Linux无人值守运行计划.md)

- [x] 文件会话来源无需 macOS 代理；原浏览器模式回归通过。
- [x] 全新 Store 与不同机器目录读取相同有效源，访问上下文稳定，操作结束清理临时 jar。
- [x] 原子替换来源后，新任务读取新内容，旧引用被拒绝。
- [x] 缺失、到期、跨域、非法格式、FIFO、符号链接、硬链接和过宽权限被拒绝。
- [x] 匿名来源、共享工作目录与动态视频号会话不能误用文件模式。
- [x] 生产受控服务按 profile 选择，普通文件来源逐平台只读挂载，默认核心拓扑无个人桌面依赖。
- [ ] 同一真实平台样本在容器重启前后均成功交付。
- [ ] 新电脑恢复个人配置、会话与业务状态后完成真实媒体交付。
- [ ] 视频号在无桌面机器的独立路径；本批未实现。

测试中的 Cookie 全部为合成数据，不代表平台端会话有效。真实授权自然过期、被撤销或出口改变导致的重新验证不以文件存在代替检查。

## 2026-09-07 验证记录

- 针对性回归：57 passed。
- 后端全量：1562 passed、2 skipped（41.85 秒）。跳过项为未提供隔离 MinIO 端点、macOS 不提供 Linux O_PATH；不算作通过。
- `ruff check app tests` 通过；`mypy app` 通过，521 个源码文件。
- 隔离 Linux 验证：UID/GID 10001，禁用网络、只读根文件系统与来源挂载、操作目录为 tmpfs。三个独立新建容器读取同一合成会话数据，均就绪、生成相同上下文、完成操作后清理临时 jar。专用测试卷已删除，未读取真实 Cookie 或重启既有业务容器。
- 另有三个全新子进程恢复验证及不同机器目录迁移回归；它们不替代真实主机冷启动与平台下载。
- 生产 Compose（含 YouTube/X profile）静态解析通过。业务镜像尚未重新部署到当前运行环境。
